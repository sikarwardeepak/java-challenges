# Isomorphic Strings
## Problem
Given two strings, determine if they are isomorphic. Two strings are isomorphic if the characters in one can be replaced to get the other, preserving order.
## Example
Input: s = "egg", t = "add"
Output: true