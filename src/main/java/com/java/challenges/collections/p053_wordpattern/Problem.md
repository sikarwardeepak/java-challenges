# Word Pattern
## Problem
Given a pattern and a string, find if string follows the same pattern. Full match means bijection between pattern letters and words.
## Example
Input: pattern = "abba", s = "dog cat cat dog"
Output: true