# Vending Machine
## Problem
Design a simple vending machine. Support insertCoin, selectProduct, dispense, and getBalance.
## Requirements
- addProduct(name, price, quantity)
- insertCoin(amount) adds to balance
- selectProduct(name) returns true if enough balance and in stock
- getBalance() returns current balance