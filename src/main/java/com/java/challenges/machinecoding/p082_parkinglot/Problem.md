# Parking Lot System
## Problem
Design a parking lot system. The parking lot has a fixed number of slots. Support park() and leave() operations.
## Requirements
- park(vehicleId) returns slot number or -1 if full
- leave(slotNumber) frees the slot
- getAvailableSlots() returns count of available slots