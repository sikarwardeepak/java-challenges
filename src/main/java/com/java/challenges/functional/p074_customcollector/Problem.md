# Custom Collector
## Problem
Create a custom collector that joins strings with a delimiter, prefix, and suffix.
## Example
Input: ["a","b","c"], delimiter=", ", prefix="[", suffix="]"
Output: "[a, b, c]"